{
    "scripts": {
        "dev": [
            "Composer\\Config::disableProcessTimeout",
            "php -S localhost:8000"
        ],
        "evolutions": [
            "php scripts/evolutions.php"
        ]
    },
    "require": {
        "twig/twig": "^3.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^12"
    }
}
